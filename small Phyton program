def multiplication_or_sum(x, y):
    product = x * y
    if product <= 1000:
        return product
    else:
        return x + y

number1 = 50
number2 = 60

print("\n")
response = multiplication_or_sum(number1, number2)
print("The response is", response)